server:
  port: 8080

spring:
  application:
    name: bookapp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-1.ceq2mnp4ry1v.us-east-2.rds.amazonaws.com:3306/bookdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: optional:application-local.yml

jwt:
  secret: aivle-bookapp-jwt-secret-key-2026!
  access-expiration: 10800000
  refresh-expiration: 86400000
